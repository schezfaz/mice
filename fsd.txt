idle_move:
1. move mouse from current point to any random point for specific
   ip(time period) and ip(time interval) between clicks

2. Loop should exit if keyboard interrupt is detect during the specified time_period